org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Same reason as maia-app/gradle.properties: this box defaults to a JDK that
# AGP 8.13 refuses at configuration time, so the JVM is named here rather than
# left to whatever JAVA_HOME a fresh shell happens to carry.
